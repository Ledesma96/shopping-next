import React from 'react';
import './logout.scss';

const Logout = () => {
    return (
        <section className='container-logout'>
            <button className='container-logout__button'>
                Cerrar sesión
            </button>
        </section>
    );
};

export default Logout;

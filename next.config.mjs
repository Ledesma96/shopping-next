/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.expoknews.com', 'celadasa.vtexassets.com', 'www.digitalsport.com.ar'],
    },
};

export default nextConfig;
